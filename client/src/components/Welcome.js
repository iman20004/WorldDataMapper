import React                from 'react';

const Welcome = (props) => {
    return (
        <div id='welcomeLabel' >
            Welcome To The World Data Mapper
        </div>
    );
};

export default Welcome;